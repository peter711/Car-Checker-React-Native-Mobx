import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    tilesContainer: {
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
    image: {
        width: 48,
        height: 48
    }
});

export default styles;